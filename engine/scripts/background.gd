extends Node2D
#
#var zoom = 1.0;
#var time = 0.0;
#
## Called when the node enters the scene tree for the first time.
#func _ready():
#	pass # Replace with function body.
#
#func _process(delta):
#	time += delta;
##	zoom = abs(cos(time * 0.1) * 1000000.0)
#	zoom = $CameraZoom.zoom.x
#	update()
#
#func update():
##	$Stars.scale = Vector2(16.0, 16.0) / (pow(zoom, 0.9))
##	$Stars2.scale = Vector2(16.0, 16.0) / (pow(zoom, 0.99))
##	$Stars3.scale = Vector2(16.0, 16.0) / (pow(zoom, 0.999))
#	$Stars.scale = Vector2(16.0, 16.0) / (pow(zoom, 0.9))
#	$Stars2.scale = Vector2(16.0, 16.0) / (pow(zoom, 0.95))
#	$Stars3.scale = Vector2(16.0, 16.0) / (pow(zoom, 0.995))
